package operator

// Run Let's do it.
func Run() {

	// https://github.com/kubernetes/sample-controller/blob/master/controller.go

	// clientset := configureClientset()
	// informer := buildConfigMapInformer(clientset, "default", "findme=foo")
	// configureConfigmapInformer(informer)

	// stopper := make(chan struct{})
	// defer runtime.HandleCrash()
	// defer close(stopper)

	// informer.Run(stopper)
}
